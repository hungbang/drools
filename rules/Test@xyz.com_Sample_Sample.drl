package com.bizlem;

import java.util.Map;

rule rules123
dialect "mvel"
when
$map: Map ($map.get("account_id") >1504112400000  && $map.get("account_id") <1524157200000  && $map.get("city")  in ("A")  && $map.get("salesPerson")  in ("Ahmadr") )
then
$map.put("Output_F1","YesF1");
end
